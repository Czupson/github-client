input {
    tcp {
        port => 5000
        codec => json_lines
    }
}
output {
    elasticsearch {
        hosts => ["http://elasticsearch:9200"]
        index => "github-client1-logs-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => rubydebug
    }
}